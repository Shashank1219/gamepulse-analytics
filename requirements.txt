# GamePulse Analytics — Python dependencies
# Requires Python 3.10+
#
# Install:
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt
#
# Airflow runs in Docker (apache/airflow:2.9.0) — not installed here.
# For local IDE type-checking of DAGs: pip install -r requirements-dev.txt

# Layer 1 — Event generator (data_generator/generate_events.py)
faker>=24.0.0
pandas>=2.0.0
pyarrow>=15.0.0
boto3>=1.28.0
python-dotenv>=1.0.0

# Layer 5 — dbt transformation (cd dbt/gamepulse && dbt run)
dbt-core>=1.11.0,<2.0.0
dbt-databricks>=1.11.0,<2.0.0
